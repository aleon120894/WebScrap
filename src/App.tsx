import React from 'react';
import RootScreen from './navigation';

const App = (): JSX.Element => {
  return (
    <React.StrictMode>
      <RootScreen />
    </React.StrictMode>
  );
};

export default App;
